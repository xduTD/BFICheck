package BFICheck;

/*
 * 输入个人信息（创建账户时） 核验 （用户）
 * 读取tag（用户）
 * 读取上一个block的信息
 * 读取第一个block的信息
 * 输出（验证）（用户）
 * 获取上一个block的hash
 * 创建起始block（用户）
 * 创建中间的block（用户）
 * 创建临时不可读取hash的block
 * check isValid 检查链是否有效
 */

public interface BFICAccount {
	
}
